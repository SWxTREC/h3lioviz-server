## Building

Navigate to https://www.paraview.org/download/ and download `ParaView-5.10.1-osmesa-MPI-Linux-Python3.9-x86_64.tar.gz` to `./docker`

## Environment Variables

Environment variables for the server are stored in `./pvw/server/env.json`. 