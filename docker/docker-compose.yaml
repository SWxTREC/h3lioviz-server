version: '2'

services:
  pvw:
    image: public.ecr.aws/enlil/paraview_web_repo:pvw-enlil-osmesa
    ports:
      - 8080:80
    restart: always
    environment:
      - SERVER_NAME=127.0.0.1:8080
      - PROTOCOL=wss
